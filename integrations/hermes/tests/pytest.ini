[pytest]
markers =
    functional: End-to-end functional tests for all 5 tools and plugin lifecycle
    reliability: Load, fault-tolerance, and edge-input tests
    security: Path traversal, injection filter, secret redaction, and env isolation tests
    ux: Availability detection, config schema, and system prompt tests
